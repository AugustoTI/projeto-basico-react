describe('<Home />', () => {
    test('teste one', () => {
        expect(1).toBe(1);
    });

    test('teste two', () => {
        expect(1).toBe(1);
    });

    test('teste three', () => {
        expect(1).toBe(1);
    });
});
